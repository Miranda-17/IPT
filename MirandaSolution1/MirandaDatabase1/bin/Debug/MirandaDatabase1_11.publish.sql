/*
Deployment script for Order

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Order"
:setvar DefaultFilePrefix "Order"
:setvar DefaultDataPath "C:\Users\Nova Miranda\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\Nova Miranda\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The type for column OrderId in table [dbo].[Order] is currently  NVARCHAR (50) NOT NULL but is being changed to  NVARCHAR (10) NOT NULL. Data loss could occur and deployment may fail if the column contains data that is incompatible with type  NVARCHAR (10) NOT NULL.
*/

IF EXISTS (select top 1 1 from [dbo].[Order])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering Table [dbo].[Order]...';


GO
ALTER TABLE [dbo].[Order] ALTER COLUMN [OrderId] NVARCHAR (10) NOT NULL;


GO
PRINT N'Altering Procedure [dbo].[CreateOrder]...';


GO
ALTER PROCEDURE [dbo].[CreateOrder]

	@OrderId NVARCHAR(10) NULL, 
    @CustomerName NVARCHAR(10) NULL, 
    @OrderDate DATE NULL, 
    @TotalAmount INT NULL, 
    @OrderStatus NVARCHAR(10) NULL
AS
BEGIN 
INSERT INTO [dbo].[Order] ([OrderId], [CustomerName], [OrderDate], [TotalAmount], [OrderStatus])
	VALUES (@OrderId, @CustomerName, @OrderDate, @TotalAmount, @OrderStatus);
END
GO
PRINT N'Altering Procedure [dbo].[DeleteOrder]...';


GO
ALTER PROCEDURE [dbo].[DeleteOrder]
		@OrderId NVARCHAR(10) =  NULL
AS
BEGIN
     Delete FROM [dbo].[Order] WHERE OrderId = @OrderId;
END
GO
PRINT N'Altering Procedure [dbo].[SelectOrder]...';


GO
ALTER PROCEDURE [dbo].[SelectOrder]
	@OrderId NVARCHAR(10) = NULL
AS
BEGIN
	SELECT * FROM [dbo].[Order] AS a WHERE a.[OrderId] = @OrderId;
END
GO
PRINT N'Altering Procedure [dbo].[UpdateOrder]...';


GO
ALTER PROCEDURE [dbo].[UpdateOrder]

    @OrderId NVARCHAR(10) NULL, 
    @CustomerName NVARCHAR(10) NULL, 
    @OrderDate DATE NULL, 
    @TotalAmount INT NULL, 
    @OrderStatus NVARCHAR(10) NULL

AS
BEGIN
     UPDATE [dbo].[Order]
     SET [CustomerName] = @CustomerName,
         [OrderDate] = @OrderDate,
         [TotalAmount] = @TotalAmount,
         [OrderStatus] = @OrderStatus
     WHERE [OrderId] = @OrderId;

END
GO
PRINT N'Refreshing Procedure [dbo].[SelectAllOrders]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[SelectAllOrders]';


GO
/*
Post-Deployment Script Template							
--------------------------------------------------------------------------------------
 This file contains SQL statements that will be appended to the build script.		
 Use SQLCMD syntax to include a file in the post-deployment script.			
 Example:      :r .\myfile.sql								
 Use SQLCMD syntax to reference a variable in the post-deployment script.		
 Example:      :setvar TableName MyTable							
               SELECT * FROM [$(TableName)]					
--------------------------------------------------------------------------------------
*/
GO

GO
PRINT N'Update complete.';


GO
